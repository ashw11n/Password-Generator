#include <iostream>
#include <vector>
#include <string>
#include <ctime>
#include <stdlib.h>

using namespace std;

int main(){

    // random password generator: 9 characters
    vector<char> overallPassword(9);
    // character option groups as elements in array
    const char* const characOptions[] = {"abcdefghijklmnopqrstuvwxyz", "ABCDEFGHIJKLMNOPQRSTUVWXYZ", "0123456789"};

    for(int i = 0; i < characOptions.size(); ++i){
        for(int j = 0; j < overallPassword.size(); ++j){





        }
    }














}